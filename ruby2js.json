// Config for babyruby2js (merged with the default config)
{
  "src": ".",
  "target": "js",
  "camelCase": true,
  
  "replacements": {
    "//require 'test/unit'": "var assertEqual = main.assertEqual",
    "var Logging = require('./Logging')": "",
    "var Logging = require('../Logging')": "",
    "var StoneConstants = require('./StoneConstants')": ""
  }
}
